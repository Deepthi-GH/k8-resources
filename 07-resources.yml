apiVersion: v1
kind: Pod
metadata:
  name: resources-demo
spec:
  containers:
  - name: nginx
    image: nginx
  resources:
    requests:
      memory: "68MI"
      cpu: "100m" #1/10th of cpu
      #provide equal or more than requests
    limits:
      memory: "100MI"  
      cpu: "120m"